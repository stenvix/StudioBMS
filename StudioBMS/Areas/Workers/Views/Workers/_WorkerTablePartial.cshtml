@inject IHtmlLocalizer<ModelResource> ModeLocalizer
@model IList<PersonModel>
@{
    Layout = "Dashboard/_TableLayout";
}

@section tableHeader{
    <th>@ModeLocalizer["FirstName"]</th>
    <th>@ModeLocalizer["LastName"]</th>
    <th>@ModeLocalizer["Position"]</th>
    <th>@ModeLocalizer["Phone"]</th>
    <th>@ModeLocalizer["WorkingHours"]</th>
    <th class="col-xs-2 text-center col-actions">@ModeLocalizer["Actions"]</th>
}

@section tableBody
{
    @foreach (var worker in Model)
    {
        <tr>
            <td>@worker.FirstName</td>
            <td>@worker.LastName</td>
            <td>
                @if (worker.Role != null)
                {
                    @ModeLocalizer[worker.Role.Name]
                }
            </td>
            <td>@worker.PhoneNumber</td>
            <td>
                @foreach (var time in worker.TimeTables)
                {
                    @Html.Raw($"{CultureInfo.CurrentCulture.DateTimeFormat.GetAbbreviatedDayName(time.WeekDay)} {time.Start:t} - {time.End:t}<br />")
                }
            </td>
            <td>
                <a class="btn btn-sm btn-primary"><i class="fa fa-bar-chart fa-fw"></i></a>
                <a class="btn btn-sm btn-primary"><i class="fa fa-clock-o fa-fw"></i></a>
                <a class="btn btn-sm btn-primary"><i class="fa fa-briefcase fa-fw"></i></a>
                <a class="btn btn-sm btn-primary"><i class="fa fa-pencil fa-fw"></i></a>
                <a class="btn btn-sm btn-danger"><i class="fa fa-trash fa-fw"></i></a>
            </td>
        </tr>
    }
}
